<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Stunden eintragen</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container">
    <h2>🕒 Stunden eintragen</h2>

    <form method="POST" action="/stunden-eintragen" class="hours-form">
      <div class="form-group">
        <label for="date">📅 Datum:</label>
        <input type="date" id="date" name="date" required />
      </div>

      <div class="form-group">
        <label for="hours">⏱️ Stunden:</label>
        <input type="number" id="hours" name="hours" step="0.25" min="0" required />
      </div>

      <div class="form-group">
        <label for="description">📝 Anmerkung (optional):</label>
        <textarea id="description" name="description" rows="3"></textarea>
      </div>

      <button type="submit" class="submit-btn">✅ Eintragen</button>
    </form>

    <div class="back-link">
      <a href="/dashboard">⬅️ Zurück zum Dashboard</a>
    </div>
  </div>
</body>
</html>
