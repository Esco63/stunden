<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard – Stundenpanel</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container">
    <header class="dashboard-header">
      <h1>👋 Willkommen, <%= user.username %>!</h1>
      <p>Du bist eingeloggt als <strong><%= user.role %></strong>.</p>
      <nav class="nav-bar">
        <button class="nav-btn" onclick="openModal()">➕ Stunden eintragen</button>
        <% if (user.role === 'admin') { %>
          <a href="/adminpanel" class="nav-btn">🔐 Adminpanel</a>
        <% } %>
        <a href="/logout" class="nav-btn logout-btn">🚪 Logout</a>
      </nav>
    </header>

    <section class="entries-section">
      <h2>📋 Deine bisherigen Einträge</h2>
      <% if (entries && entries.length > 0) { %>
        <table class="entries-table">
          <thead>
            <tr>
              <th>📅 Datum</th>
              <th>⏱️ Stunden</th>
              <th>📝 Beschreibung</th>
            </tr>
          </thead>
          <tbody>
            <% entries.forEach(entry => { %>
              <tr>
                <td><%= entry.date %></td>
                <td><%= entry.hours %></td>
                <td><%= entry.description %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } else { %>
        <p class="no-entries">⚠️ Du hast noch keine Einträge gemacht.</p>
      <% } %>
    </section>
  </div>

  <!-- Modal -->
  <div id="stundenModal" class="modal-overlay">
    <div class="modal-content">
      <h2>🕒 Stunden eintragen</h2>
      <form method="POST" action="/stunden-eintragen">
        <input type="date" name="date" required />
        <input type="number" name="hours" step="0.1" placeholder="Stunden" required />
        <textarea name="description" rows="3" placeholder="Beschreibung (optional)"></textarea>
        <button type="submit" class="submit-btn">💾 Speichern</button>
        <button type="button" onclick="closeModal()" class="cancel-btn">Abbrechen</button>
      </form>
    </div>
  </div>

  <script>
    function openModal() {
      document.getElementById("stundenModal").classList.add("active");
    }

    function closeModal() {
      document.getElementById("stundenModal").classList.remove("active");
    }

    // ESC zum Schließen
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") closeModal();
    });

    // Klick außerhalb schließt Modal
    window.addEventListener("click", (e) => {
      const modal = document.getElementById("stundenModal");
      if (e.target === modal) closeModal();
    });
  </script>
</body>
</html>
